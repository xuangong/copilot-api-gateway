/**
 * Re-export of the framework HTTPError. The real class lives in
 * @vibe-core/upstream — bridge for existing `@vibe-llm/provider-llm/errors`
 * import paths through Spec 9 Part 2.
 */
export { HTTPError } from '@vibe-core/upstream'

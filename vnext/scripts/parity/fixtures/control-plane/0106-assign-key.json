{
  "name": "assign-key",
  "endpoint": "/api/keys/${capture.create-key.keyId}/assign",
  "method": "POST",
  "auth": "admin-session",
  "dependsOn": ["create-key"],
  "body": { "user_id": "${env.PARITY_TARGET_USER_ID}" },
  "expect_status": 200
}

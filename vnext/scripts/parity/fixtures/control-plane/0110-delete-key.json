{
  "name": "delete-key",
  "endpoint": "/api/keys/${capture.create-key.keyId}",
  "method": "DELETE",
  "auth": "admin-session",
  "dependsOn": ["unassign-key", "copy-web-search-from"],
  "expect_status": 200
}

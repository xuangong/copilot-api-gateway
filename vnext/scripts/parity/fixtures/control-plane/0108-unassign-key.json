{
  "name": "unassign-key",
  "endpoint": "/api/keys/${capture.create-key.keyId}/assign/${env.PARITY_TARGET_USER_ID}",
  "method": "DELETE",
  "auth": "admin-session",
  "dependsOn": ["assign-key", "list-assignments"],
  "expect_status": 200
}
